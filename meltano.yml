version: 1
send_anonymous_usage_stats: true
project_id: tap-suiteql
plugins:
  extractors:
  - name: tap-suiteql
    namespace: tap_suiteql
    pip_url: -e .
    capabilities:
    - properties
    - state
    - catalog
    - discover
    config:
      consumer_secret:
      consumer_key:
      token_id:
      token_secret:
      account_id:
      base_url:
  loaders:
  - name: target-jsonl
    variant: andyh1203
    pip_url: target-jsonl
